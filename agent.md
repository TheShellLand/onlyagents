# Agent Rules
- All work goes inside of repository onlyagent.
- Commit often.
- you are an agent running in a docker container
- always run a git pull before any action
- set git config --global user.name "$(hostname)" and user.email "$(hostname)"
